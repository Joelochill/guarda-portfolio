---
import { PortableText } from 'astro-portabletext';
import { aboutContentEs } from '../cms/queries';
import Layout from '../layouts/Layout.astro';

const seo = {
  title: 'About',
  description:
    'Descubre el proyecto GUARDA de Joel Rico Rada, explorando el terror espiritual con field recordings, síntesis modular y spoken word.',
  openGraph: {
    basic: {
      title: 'About | GUARDA',
    },
  },
};
---

<Layout seo={seo}>
  <main transition:name="main" transition:animate="fade">
    <PortableText value={aboutContentEs} />
  </main>
</Layout>

<style>
  main {
    gap: 1.5em;
    align-content: start;
    max-inline-size: 110ch;
  }
</style>
